/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enables MDX file handling if we were to use standard @next/mdx, 
  // but we are using next-mdx-remote/rsc, so no specific config is strictly required here.
  // We'll keep it standard.
};

export default nextConfig;
